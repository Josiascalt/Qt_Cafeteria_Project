/*#pragma once

#include <deque>

#include "domain.h"

namespace cafeteria_app {
    namespace database {
        using namespace domain::types;

        class RecordCatalogue {
        public:
            RecordCatalogue(...);
            ~RecordCatalogue();
        private:
            std::deque<domain::types::Session> sessions_;
            std::deque<domain::interfaces::Identifiable::Type> buffer_;
        };
    } //namespace database
} //namespace cafeteria_app*/


